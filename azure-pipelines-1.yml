---
trigger:
  - main
stages:
  - stage: build
    displayName: Build Stage
    pool: 
      name: Default
      demands:
        - Maven
        - Agent.Version -gtVersion 3.236.1


    jobs:
      -  job: maven
         displayName: Build using maven
         steps:
           - task: Maven@4
             inputs:
               mavenPomFile: 'pom.xml' 
               goals: package
